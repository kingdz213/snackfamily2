# Scripts SnackFamily2

## Générer les prompts d'images

Ce script lit le menu (`data/menuData.ts`) et génère un CSV de prompts d’images.

```bash
node scripts/generate_image_prompts.mjs
```

Le fichier est généré dans `scripts/image-prompts.csv`.
