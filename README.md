# Configuration rapide

- Variables front à définir sur Vercel : `VITE_STRIPE_PUBLIC_KEY`, `VITE_WORKER_URL`, `VITE_FIREBASE_VAPID_KEY`.
- Secrets Stripe côté Firebase Functions (à ne pas mettre dans le front) : `STRIPE_API_KEY`, `STRIPE_WEBHOOK_SECRET`.
